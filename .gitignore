# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.pnpm-store

# Dist / Build (ผลลัพธ์จากการ Build)
dist
dist-ssr
*.local

# Editor directories and files (ไฟล์ตั้งค่าของโปรแกรมเขียนโค้ด)
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# Environment Variables (สำคัญมาก ห้ามหลุดขึ้น Git เพื่อความปลอดภัย)
.env
.env.*
!.env.example

# Testing
coverage

# Temporary files
*.tmp

# --- ส่วนที่แนะนำให้เพิ่ม ---

# Vite specific (ไฟล์ขยะที่เกิดจาก Vite)
vite.config.ts.timestamp-*
vite.config.js.timestamp-*

# TypeScript (Cache ของ TypeScript)
*.tsbuildinfo

# ESLint (Cache ของตัวตรวจโค้ด)
.eslintcache

# System Files (ไฟล์ขยะของระบบปฏิบัติการ)
Thumbs.db
ehthumbs.db
Desktop.ini